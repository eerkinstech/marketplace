export const metadata = {
  title: "Checkout",
  description: "Complete your marketplace order securely.",
  robots: {
    index: false,
    follow: false
  }
};

export default function CheckoutLayout({ children }) {
  return children;
}
