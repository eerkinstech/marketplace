export const metadata = {
  title: "Wishlist",
  description: "View products saved to your marketplace wishlist.",
  robots: {
    index: false,
    follow: false
  }
};

export default function WishlistLayout({ children }) {
  return children;
}
