export const metadata = {
  title: "Returns",
  description: "Submit a product return request and learn how marketplace returns are handled.",
  alternates: {
    canonical: "/returns"
  },
  openGraph: {
    type: "website",
    title: "Returns",
    description: "Submit a product return request and learn how marketplace returns are handled.",
    url: "/returns"
  }
};

export default function ReturnsLayout({ children }) {
  return children;
}
