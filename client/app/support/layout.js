export const metadata = {
  title: "Support",
  description: "Open vendor support conversations and manage marketplace help requests.",
  robots: {
    index: false,
    follow: false
  }
};

export default function SupportLayout({ children }) {
  return children;
}
