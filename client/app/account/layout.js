export const metadata = {
  title: "Account",
  description: "Manage account details, orders, returns, and support conversations.",
  robots: {
    index: false,
    follow: false
  }
};

export default function AccountLayout({ children }) {
  return children;
}
